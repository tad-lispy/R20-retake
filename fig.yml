web:
  build: .
  ports:
    - "3210:3210"
  volumes:
    - .:/app
  links:
    - mongo
    - elasticsearch

mongo:
  image: mongo
  command:  [
    "mongod",
    "--quiet",
    "--replSet",
    "r20"
  ]

elasticsearch:
  image: lzrski/elasticsearch
